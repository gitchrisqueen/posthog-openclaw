name: 'Release'

on:
    push:
        branches: [main]
        paths:
            - '.changeset/*.md'
    workflow_dispatch:

permissions:
    contents: read

# Concurrency control: only one release process can run at a time
# This prevents race conditions if multiple releasable changesets merge simultaneously
concurrency:
    group: release
    cancel-in-progress: false

jobs:
    check-changesets:
        name: Check for changesets
        runs-on: ubuntu-latest
        outputs:
            has-changesets: ${{ steps.check.outputs.has-changesets }}
        steps:
            - name: Checkout repository
              uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
              with:
                  ref: main
                  fetch-depth: 0

            - name: Check for changesets
              id: check
              run: |
                  changeset_count=$(find .changeset -name '*.md' ! -name 'README.md' 2>/dev/null | wc -l)
                  if [ "$changeset_count" -gt 0 ]; then
                    echo "has-changesets=true" >> "$GITHUB_OUTPUT"
                    echo "Found $changeset_count changeset(s), ready to release"
                  else
                    echo "has-changesets=false" >> "$GITHUB_OUTPUT"
                    echo "No changesets to release"
                  fi

    notify-approval-needed:
        name: Notify Slack - Approval Needed
        needs: check-changesets
        if: needs.check-changesets.outputs.has-changesets == 'true'
        uses: posthog/.github/.github/workflows/notify-approval-needed.yml@5fc4680761e8ac29a61b212756230eba0e276d8c # main
        with:
            slack_channel_id: ${{ vars.SLACK_APPROVALS_CLIENT_LIBRARIES_CHANNEL_ID }}
            slack_user_group_id: ${{ vars.GROUP_CLIENT_LIBRARIES_SLACK_GROUP_ID }}
        secrets:
            slack_bot_token: ${{ secrets.SLACK_CLIENT_LIBRARIES_BOT_TOKEN }}
            posthog_project_api_key: ${{ secrets.POSTHOG_PROJECT_API_KEY }}

    version-bump:
        name: Bump version and commit to main
        needs: [check-changesets, notify-approval-needed]
        runs-on: ubuntu-latest
        if: always() && needs.check-changesets.outputs.has-changesets == 'true'
        environment: 'Release'
        permissions:
            contents: write
        outputs:
            committed: ${{ steps.commit-version-bump.outputs.commit-hash != '' }}
            new-version: ${{ steps.apply-changesets.outputs.new-version }}
        steps:
            - name: Notify Slack - Approved
              continue-on-error: true
              uses: posthog/.github/.github/actions/slack-thread-reply@cb0979b67dcd585828b61ac45927eef4da8f6287 # main
              with:
                  slack_bot_token: ${{ secrets.SLACK_CLIENT_LIBRARIES_BOT_TOKEN }}
                  slack_channel_id: ${{ vars.SLACK_APPROVALS_CLIENT_LIBRARIES_CHANNEL_ID }}
                  thread_ts: ${{ needs.notify-approval-needed.outputs.slack_ts }}
                  message: '✅ Release approved! Version bump in progress...'
                  emoji_reaction: 'white_check_mark'

            - name: Get GitHub App token
              id: releaser
              uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2.2.2
              with:
                  app-id: ${{ secrets.GH_APP_POSTHOG_OPENCLAW_RELEASER_APP_ID }}
                  private-key: ${{ secrets.GH_APP_POSTHOG_OPENCLAW_RELEASER_PRIVATE_KEY }}

            - name: Checkout repository
              uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
              with:
                  ref: main
                  fetch-depth: 0
                  token: ${{ steps.releaser.outputs.token }}

            - name: Configure Git
              run: |
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"

            - name: Setup environment
              uses: ./.github/actions/setup

            - name: Apply changesets and update version
              id: apply-changesets
              run: |
                  pnpm changeset version
                  new_version=$(node -p "require('./package.json').version")
                  echo "new-version=$new_version" >> "$GITHUB_OUTPUT"

            - name: Sync version.ts
              env:
                  NEW_VERSION: ${{ steps.apply-changesets.outputs.new-version }}
              run: |
                  sed -i "s/export const VERSION = '.*'/export const VERSION = '${NEW_VERSION}'/" src/version.ts

            - name: Update lockfile
              run: pnpm install --no-frozen-lockfile

            - name: Check for version bump changes
              id: check-changes
              run: |
                  if [ -z "$(git status --porcelain)" ]; then
                    echo "No changes to commit"
                    echo "committed=false" >> "$GITHUB_OUTPUT"
                  else
                    echo "committed=true" >> "$GITHUB_OUTPUT"
                  fi

            - name: Commit version bump and lockfile
              id: commit-version-bump
              if: steps.check-changes.outputs.committed == 'true'
              uses: planetscale/ghcommit-action@25309d8005ac7c3bcd61d3fe19b69e0fe47dbdde # v0.2.20
              with:
                  commit_message: 'chore: release ${{ steps.apply-changesets.outputs.new-version }} [version bump] [skip ci]'
                  repo: ${{ github.repository }}
                  branch: main
              env:
                  GITHUB_TOKEN: ${{ steps.releaser.outputs.token }}

            - name: Send failure event to PostHog
              if: ${{ failure() }}
              continue-on-error: true
              uses: PostHog/posthog-github-action@9a6a19d360820ab4d95ecc4a5a7564062c895f84 # v1.3.0
              with:
                  posthog-token: '${{ secrets.POSTHOG_PROJECT_API_KEY }}'
                  event: 'posthog-sdk-github-release-workflow-failure'
                  properties: >-
                      {
                        "commitSha": "${{ github.sha }}",
                        "jobStatus": "${{ job.status }}",
                        "ref": "${{ github.ref }}",
                        "repository": "${{ github.repository }}",
                        "sdk": "posthog-openclaw",
                        "jobName": "version-bump",
                        "packageName": "@posthog/openclaw",
                        "packageVersion": "${{ steps.apply-changesets.outputs.new-version || 'unknown' }}",
                        "actionUrl": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                        "alertText": ":rotating_light: Failed to prepare release @posthog/openclaw@${{ steps.apply-changesets.outputs.new-version }} :rotating_light:",
                        "alertContext": "The version bump/changelog step failed before packages were published."
                      }

    notify-rejected:
        name: Notify Slack - Rejected
        needs: [version-bump, notify-approval-needed]
        runs-on: ubuntu-latest
        if: always() && needs.version-bump.result == 'failure' && needs.notify-approval-needed.outputs.slack_ts != ''
        steps:
            - name: Check for rejection
              id: check-rejection
              env:
                  GH_TOKEN: ${{ github.token }}
              run: |
                  RESPONSE=$(gh api /repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/approvals)
                  REJECTED=$(echo "$RESPONSE" | jq '[.[] | select(.state == "rejected")] | length')
                  if [ "$REJECTED" -gt 0 ]; then
                    echo "was_rejected=true" >> "$GITHUB_OUTPUT"
                    COMMENT=$(echo "$RESPONSE" | jq -r '.[] | select(.state == "rejected") | .comment // empty' | head -1)
                    if [ -n "$COMMENT" ]; then
                      {
                        echo 'message<<EOF'
                        echo "🚫 Release was rejected: $COMMENT"
                        echo 'EOF'
                      } >> "$GITHUB_OUTPUT"
                    else
                      echo "message=🚫 Release was rejected." >> "$GITHUB_OUTPUT"
                    fi
                  else
                    echo "was_rejected=false" >> "$GITHUB_OUTPUT"
                  fi

            - name: Notify Slack - Rejected
              if: steps.check-rejection.outputs.was_rejected == 'true'
              continue-on-error: true
              uses: posthog/.github/.github/actions/slack-thread-reply@cb0979b67dcd585828b61ac45927eef4da8f6287 # main
              with:
                  slack_bot_token: ${{ secrets.SLACK_CLIENT_LIBRARIES_BOT_TOKEN }}
                  slack_channel_id: ${{ vars.SLACK_APPROVALS_CLIENT_LIBRARIES_CHANNEL_ID }}
                  thread_ts: ${{ needs.notify-approval-needed.outputs.slack_ts }}
                  message: '${{ steps.check-rejection.outputs.message }}'
                  emoji_reaction: 'no_entry_sign'

    publish:
        name: Publish to NPM
        needs: [version-bump, notify-approval-needed]
        runs-on: ubuntu-latest
        if: always() && needs.version-bump.outputs.committed == 'true'
        permissions:
            contents: write
            id-token: write
        steps:
            - name: Checkout repository
              uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
              with:
                  ref: main
                  fetch-depth: 0

            - name: Configure Git
              run: |
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"

            - name: Setup environment
              uses: ./.github/actions/setup

            - name: Check package version
              id: check-package-version
              uses: PostHog/check-package-version@f540540cb12d45ec3c58d42dbf1d60070ed2d268 # v2.1.1

            - name: Typecheck
              if: steps.check-package-version.outputs.is-new-version == 'true'
              run: pnpm typecheck

            - name: Test
              if: steps.check-package-version.outputs.is-new-version == 'true'
              run: pnpm test

            - name: Tag repository
              if: steps.check-package-version.outputs.is-new-version == 'true'
              env:
                  COMMITTED_VERSION: ${{ steps.check-package-version.outputs.committed-version }}
              run: |
                  git tag -a "$COMMITTED_VERSION" -m "$COMMITTED_VERSION"

            - name: Publish to NPM
              if: steps.check-package-version.outputs.is-new-version == 'true'
              run: pnpm publish --access public --no-git-checks
              env:
                  NPM_CONFIG_PROVENANCE: true

            - name: Push tag to GitHub
              if: steps.check-package-version.outputs.is-new-version == 'true'
              env:
                  COMMITTED_VERSION: ${{ steps.check-package-version.outputs.committed-version }}
              run: |
                  git push origin "$COMMITTED_VERSION"

            - name: Create GitHub Release
              if: steps.check-package-version.outputs.is-new-version == 'true'
              env:
                  GH_TOKEN: ${{ github.token }}
                  COMMITTED_VERSION: ${{ steps.check-package-version.outputs.committed-version }}
              run: |
                  LAST_CHANGELOG_ENTRY=$(awk -v defText="see CHANGELOG.md" '/^## /{if (flag) exit; flag=1; next} flag && /^##$/{exit} flag; END{if (!flag) print defText}' CHANGELOG.md)
                  gh api \
                    --method POST \
                    -H "Accept: application/vnd.github+json" \
                    -H "X-GitHub-Api-Version: 2022-11-28" \
                    /repos/${{ github.repository }}/releases \
                    -f tag_name="$COMMITTED_VERSION" \
                    -f target_commitish='main' \
                    -f name="$COMMITTED_VERSION" \
                    -f body="$LAST_CHANGELOG_ENTRY" \
                    -F draft=false \
                    -F prerelease=false \
                    -F generate_release_notes=false

            - name: Send failure event to PostHog
              if: ${{ failure() }}
              continue-on-error: true
              uses: PostHog/posthog-github-action@9a6a19d360820ab4d95ecc4a5a7564062c895f84 # v1.3.0
              with:
                  posthog-token: '${{ secrets.POSTHOG_PROJECT_API_KEY }}'
                  event: 'posthog-sdk-github-release-workflow-failure'
                  properties: >-
                      {
                        "commitSha": "${{ github.sha }}",
                        "jobStatus": "${{ job.status }}",
                        "ref": "${{ github.ref }}",
                        "repository": "${{ github.repository }}",
                        "sdk": "posthog-openclaw",
                        "jobName": "publish",
                        "packageName": "@posthog/openclaw",
                        "packageVersion": "${{ steps.check-package-version.outputs.committed-version || 'unknown' }}",
                        "actionUrl": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                        "alertText": ":rotating_light: Failed to release @posthog/openclaw@${{ steps.check-package-version.outputs.committed-version }} :rotating_light:",
                        "alertContext": "The package publish step failed."
                      }

            - name: Notify Slack - Failed
              continue-on-error: true
              if: ${{ failure() && needs.notify-approval-needed.outputs.slack_ts != '' }}
              uses: posthog/.github/.github/actions/slack-thread-reply@cb0979b67dcd585828b61ac45927eef4da8f6287 # main
              with:
                  slack_bot_token: ${{ secrets.SLACK_CLIENT_LIBRARIES_BOT_TOKEN }}
                  slack_channel_id: ${{ vars.SLACK_APPROVALS_CLIENT_LIBRARIES_CHANNEL_ID }}
                  thread_ts: ${{ needs.notify-approval-needed.outputs.slack_ts }}
                  message: '❌ Failed to release `@posthog/openclaw@${{ steps.check-package-version.outputs.committed-version }}`! <https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>'
                  emoji_reaction: 'x'

    notify-released:
        name: Notify Slack - Released
        needs: [notify-approval-needed, publish]
        runs-on: ubuntu-latest
        if: always() && needs.publish.result == 'success' && needs.notify-approval-needed.outputs.slack_ts != ''
        steps:
            - name: Notify Slack - Released
              continue-on-error: true
              uses: posthog/.github/.github/actions/slack-thread-reply@cb0979b67dcd585828b61ac45927eef4da8f6287 # main
              with:
                  slack_bot_token: ${{ secrets.SLACK_CLIENT_LIBRARIES_BOT_TOKEN }}
                  slack_channel_id: ${{ vars.SLACK_APPROVALS_CLIENT_LIBRARIES_CHANNEL_ID }}
                  thread_ts: ${{ needs.notify-approval-needed.outputs.slack_ts }}
                  message: '🚀 `@posthog/openclaw` released successfully!'
                  emoji_reaction: 'rocket'
