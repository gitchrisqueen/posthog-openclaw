{
    "id": "posthog",
    "name": "PostHog LLM Analytics",
    "description": "Send LLM traces, generations, and tool spans to PostHog",
    "configSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
            "apiKey": {
                "type": "string",
                "description": "PostHog project API key. Optional if POSTHOG_API_KEY is set."
            },
            "host": {
                "type": "string",
                "default": "https://us.i.posthog.com",
                "description": "PostHog instance URL. Optional if POSTHOG_HOST is set; otherwise defaults to https://us.i.posthog.com."
            },
            "privacyMode": {
                "type": "boolean",
                "default": false,
                "description": "When enabled, LLM input/output content is not sent to PostHog"
            },
            "traceGrouping": {
                "type": "string",
                "enum": ["message", "session"],
                "default": "message",
                "description": "Trace grouping mode. 'message': one trace per runId (default). 'session': group all generations in a session into one trace, splitting on timeout."
            },
            "sessionWindowMinutes": {
                "type": "number",
                "default": 60,
                "description": "Minutes of inactivity before starting a new session window. Applies to both trace grouping modes."
            },
            "enabled": { "type": "boolean", "default": true }
        }
    },
    "uiHints": {
        "apiKey": {
            "label": "PostHog API Key",
            "sensitive": true,
            "placeholder": "phc_...",
            "help": "Optional if POSTHOG_API_KEY is set in the environment."
        },
        "host": {
            "label": "PostHog Host",
            "help": "Your PostHog instance URL. Optional if POSTHOG_HOST is set in the environment."
        },
        "privacyMode": {
            "label": "Privacy Mode",
            "help": "When enabled, LLM input/output content is not sent to PostHog"
        },
        "traceGrouping": {
            "label": "Trace Grouping",
            "help": "How to group LLM generations into traces. 'message' creates a new trace per message, 'session' groups an entire conversation into one trace."
        },
        "sessionWindowMinutes": {
            "label": "Session Window (minutes)",
            "help": "Minutes of inactivity before starting a new session window. Applies to both trace grouping modes."
        }
    },
    "schemaCacheKey": "posthog-v1"
}
